import React from "react";
import ReactDOM from "react-dom/client";
import JumpDiffusionVisualizer from "../jump_visualizer";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <JumpDiffusionVisualizer />
  </React.StrictMode>
);
